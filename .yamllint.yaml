extends: default

rules:
  document-start: disable
  line-length:
    max: 200
    level: warning
  truthy:
    check-keys: false
  comments:
    min-spaces-from-content: 1
  comments-indentation: disable

ignore: |
  node_modules/
  dist/
  pnpm-lock.yaml
