# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*~

# Environment
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Build
dist/
build/

# Logs
*.log

# Bun --compile binaries (committed only at release time via CI, repo-internal ADR-0001)
packages/gh-tasks/bin/gh-tasks-*

# Re-include team-shared VS Code config from commons sync
# (overrides any global .vscode/ ignore in user's ~/.gitignore_global)
!.vscode/
!.vscode/extensions.json
!.vscode/settings.json
