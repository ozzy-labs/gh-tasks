# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*~

# Environment
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Build
dist/
build/

# Logs
*.log

# Bun --compile binaries (release binaries are attached to GitHub Releases by
# CI; dev binary is the host-only `pnpm run build` output. Neither is committed
# — repo-internal ADR-0001).
packages/gh-tasks/bin/

# Go build artifacts (Go 移行: ADR-0006)
/gh-tasks-bin
/bin/
*.test
*.out
coverage.txt
coverage.html

# Re-include team-shared VS Code config from commons sync
# (overrides any global .vscode/ ignore in user's ~/.gitignore_global)
!.vscode/
!.vscode/extensions.json
!.vscode/settings.json
